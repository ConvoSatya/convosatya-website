import type { Metadata } from "next";
import DataEngineerInternPage from "@/components/DataEngineerInternPage";

export const metadata: Metadata = {
    title: "Data Analyst / Data Engineer Intern | ConvoSatya Careers",
    description: "Join ConvoSatya as a Data Analyst / Data Engineer Intern — remote, Summer 2026.",
};

export default function DataEngineerIntern() {
    return <DataEngineerInternPage />;
}